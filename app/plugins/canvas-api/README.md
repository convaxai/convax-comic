# @convax/canvas-api

Contract-only Canvas package for Convax Comic. It defines the schema-version 2 JSON project/document model, secure constrained patches, host/client registration APIs, revision waiters, committed events, and Cordis service declarations.

The persisted contract intentionally has no React Flow runtime dependency. React is used only as a peer type for client renderer component registrations.
